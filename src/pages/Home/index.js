import React from 'react';
import LogoutButton from '../../components/Logout';

const Home = () => {
    return (
        <div>
            <h1>Envio de Arquivos</h1>
            <LogoutButton />
        </div>
    );
};

export default Home;
